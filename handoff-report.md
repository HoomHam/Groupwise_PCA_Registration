# Handoff — 2026-09-17 (session 4 close, regenerated by /leave)
## Project: 2026_PCA_registration — Xenon-129 Lung MRI Registration Pipeline

History: `workspace/canon/ledger.md` (session 4 = 2026-09-17 03:15 → 19:20) · validity:
`workspace/canon/facts.md` (F1–F65) · map: `workspace/canon/tree.md` (★ B15, B19 opened) ·
scripts: `workspace/canon/cards.md` (C1–C29). Previous handoff: `workspace/handoffs/handoff-2026-09-17.md`.

## State

- **ANTs batch (C24) running on Ext, detached**, single driver `ants_v11_2026-09-16/_run_batch1b.sh`
  (caffeinate/nohup), log `_batch1.log`. Done: 010AJ (legacy_corrupt reference only), 018CS.
  Queue: 000LL (ourpca2 in flight) → 011CN → 015HK → 039CP; ~3 h per subject-method; ETA all
  done 2026-09-18 ~18:00. `BATCH2 DONE` is echoed into `_batch1.log` and relayed into `_batch2.log`
  for the kymo refresher. Batch died once today (12:22, Ext re-mount) — if it died again: rerun
  `_run_batch1b.sh` (skip-if-done + per-bin checkpoints). Watchers: log/file greps only, never
  `pgrep -f` your own pattern. Ext 🔴 — do not unplug HoomHamExt. WorkVault currently unmounted.
- **Verdict so far (F64, n = 1 clean):** legacy final still ahead of ourpca2+ANTs / Elastix+ANTs on
  every metric (018CS); the ANTs passes bring both to legacy_orig level and equalise them.
  Confound: our ANTs at 1×, legacy 2× — Hooman wants a 2× check on one subject later.
- **Corrupt-legacy rule (C29, F65):** 5 studies (010AJ, 003KH ×2, 001JM 2024-01-18, 009JT
  2024-02-27) out of every cohort aggregate, files renamed `_corrupt`; C21/C22/C25/C26/C27/C28
  all import `legacy_quality.py`. Never pick them for ANTs runs.
- **Scoring wired:** C21 `compare_methods.py` + C22 `pairwise_bins.py` carry `ourpca2_ants` /
  `elastix_ants` (+ `_ants_orig` in C21); C26 kymo already had them. Rerun all three when a subject
  lands. Tables: `outputs/score_cohort_2026-09-16/{compare_all.md, pairwise/pairwise_summary.md,
  kymo/linemetrics_summary.md}`.
- **Figures made today:** v11 stage videos (C27, 64 mp4 + summaries, `outputs/stage_video_2026-09-17/`;
  2 dirs need WorkVault), 4-row cines (C28, `outputs/cine_panel_2026-09-17/`), per-study pairwise
  triangles for 010AJ/018CS.
- **Lineage established:** root script is v15-derived, not v11 (F12 F13 F41 F46 are known
  divergences); archive holds v0.0 → v15.0.
- Env: Elastix + antspy in repo `.venv` (py3.11); MATLAB R2025a for CLAHE (C23 wrapper can hang
  after MATLAB exits — kill and convert `clahe.mat` by hand); scorers/figures run in base.

## Next steps

1. **Next session (Hooman): "another figure I need you to build" — unrelated to the digging.**
2. As ANTs subjects land: rerun C21, C22 (`--fig STUDY`), C26; C28 cines for each; update F64.
3. After Hooman's talk — B19 legacy-lineage audit, his four items in order:
   (1) which WorkVault `reg/<sub>` result ← which script version; (2) changes per version after v11;
   (3) why the v15-derived root gives impossible lung deformations on gas; (4) which version made the
   corrupt dark bins. Inputs listed in tree.md B19.
4. 2× ANTs check on 018CS (one subject-method ≈ a day of compute).
5. B17 `_merged` registrations; cohort labels → `cohort_map.csv`; B13 composition audit.

Parked: quiver plots (F39), ANTs MI (F18), production iterations (F14), F12/F13, dissolved-phase warping.
