# Handoff — 2026-09-17 (session 3 close, regenerated by /leave)
## Project: 2026_PCA_registration — Xenon-129 Lung MRI Registration Pipeline

History: `workspace/canon/ledger.md` (entries 2026-09-15 → 09-17) · validity: `workspace/canon/facts.md`
(F1–F61) · map: `workspace/canon/tree.md` (★ B15) · scripts: `workspace/canon/cards.md` (C1–C26).
Previous handoff: `workspace/handoffs/handoff-2026-09-12.md`.

## State

- **Cohort comparison done on one mask** (100 usable Tyger dynamics): XeCS ourpca2 (104, Ext
  `xecs_registrations_2026-09-15/`), Elastix one-shot C18 (103, Ext `elastix_oneshot_2026-09-16/`),
  legacy hierarchical+ANTs stages (final / orig / clahe / groupwise, `ns_` merged, Ext
  `Work/Analysis/` read-only). Tables: `workspace/outputs/score_cohort_2026-09-16/{summary.md,
  compare_all.md, pairwise/pairwise_summary.md, kymo/linemetrics_summary.md}`.
- **Verdicts** (F54–F61): Elastix wins temporal CV / COM stroke / per-line stroke; ourpca2 wins
  D_PCA2 / rank-3 but under-deforms (captures ~45 % of the lung-volume change, det J is physiology,
  F56); legacy final leads every Casorati metric because of its pairwise ANTs EI passes; the legacy
  groupwise stage alone is the worst run (F58). F49's "Elastix wins on lung" was 002BB-only.
- **Running on Ext, detached** (survives session exit, NOT a reboot): the exact legacy v11 ANTs
  passes (MATLAB CLAHE C23 + port C24) on top of ourpca2 and Elastix for 6 subjects (010AJ, 018CS,
  000LL, 039CP; then 011CN, 003KH), 1×, 3 passes, per-bin checkpoints. Logs
  `ants_v11_2026-09-16/_batch{1,2}.log`; batch 2 auto-starts; ETA 2026-09-18 evening. A refresher
  (`_kymo_refresh.sh`) adds the ANTs rows to the kymograph figures as they land.
  If the laptop rebooted: rerun `_run_batch1.sh` (resumes from checkpoints), re-arm batch 2 and the
  refresher. Watchers must use log greps, never `pgrep -f` their own pattern.
- **Kymograph figures** (Hooman's layout, C25/C26): all 103 subjects × coronal + sagittal, with
  per-line stroke / rank1 / D_PCA2 / tstd_cv, in `outputs/score_cohort_2026-09-16/kymo/`.
- **Merged-dynamic recons** (F60): `_merged` preferred for 030DN 03-06, 013VM 08-12, 008CR 01-31;
  no registration on them yet. `notes/Recon_Inputs_2026-09-17.md`.
- **Inputs from other sessions** (copies in `workspace/notes/`): XeCS registration manifests,
  CALSPEC package (`calspec_package_2026-09-16/`, confirmed cohort map), legacy CLAHE.m
  (`workspace/reference/`).
- Env: Elastix + antspy in repo `.venv` (py3.11); MATLAB R2025a batch for CLAHE; scorers run in base.

## Next steps

1. When `_batch2.log` has `BATCH2 DONE`: add `ourpca2_ants` / `elastix_ants` as runs in C21
   (`compare_methods.py`), C22 (`pairwise_bins.py`), rerun C26; state where they land vs legacy
   final / orig as a fact.
2. Elastix one-shot (C18) + ourpca2 (XeCS lane) on the three `_merged` recons as separate rows.
3. Switch cohort labels in C21/C22 to `notes/calspec_package_2026-09-16/cohort_map.csv`.
4. Hierarchical composition audit (F46) — still untouched.

Parked: quiver plots (F39), ANTs MI (F18), production iterations (F14), F12/F13, dissolved-phase warping.

Ext at exit: 🔴 live writers (ANTs batch + refresher) — do not unplug HoomHamExt.
