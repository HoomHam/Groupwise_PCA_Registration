# Handoff — 2026-09-12 (session 2, regenerated by /leave)
## Project: 2026_PCA_registration — Xenon-129 Lung MRI Registration Pipeline

History: `workspace/canon/ledger.md` · validity: `workspace/canon/facts.md` (F1–F50) ·
map: `workspace/canon/tree.md` · scripts: `workspace/canon/cards.md` (C1–C17).
Previous handoff: `workspace/handoffs/handoff-2026-07-06.md`.

## State

- **`ourpca2/` at repo root** = XeCS's working native one-shot groupwise PCA registrant
  (numpy D_PCA2 + trilinear lattice + L-BFGS-B), copied 2026-09-12 with provenance
  (`ourpca2/README.md`). Gradient FD-verified. Runs on our data (C12/C13).
- **Benchmark harness**: `workspace/helpers/scripts/score_all.py` (C15) scores any number
  of runs on one mask; `compare_49_three.py` (C14) = 3-run version with figure;
  `make_video_bins.py` (C17) = per-slice, bin-animated, side-by-side videos.
- **Results** (F45, F48–F50; JSON in `workspace/outputs/compare_002BB_three/score_all_002BB.json`):
  on human 002BB, Elastix one-shot 16 removes the most motion; ourpca2 is the smoothest
  field / lowest rank-3 residual but sits 0.2 D_PCA2 behind and more iterations do not
  close it (F49); hierarchical 4→7→14→16 is worst on every Casorati metric.
- **Defect found**: hierarchical output geometry is biased to/past end-inhale on three
  datasets and two grid pitches (F46). Composition across phases 3→4/5→7 suspected.
  Nothing started.
- **F41/F47 fixed**: `FinalGridSpacingInPhysicalUnits` key corrected in
  `register_groupwise_PCA_step.py:455` and C5. Fixed-pitch runs carry `_gs6`; they help
  one-shot slightly but det J min falls to 0.06 (F50).
- **Data facts**: `workspace/data/recon/49.mat` = MID00049 piston PHANTOM, `23.mat` =
  human 023LL (F44). `002BB.mat` is a symlink to the Tyger recon on HoomHamExt.
- **Env**: Elastix scripts need repo `.venv` (py3.11, SimpleElastix); base conda has no
  SimpleITK. ourpca2 runs in base.
- All runs today: raw `gas_phase` only, no CLAHE/enhance/denoise.

## Next steps

1. **Hierarchical composition audit (F46)** — per-bin COM diagnostics are in the ledger;
   start at `apply_transformations_in_sequence` / phase 5 apply-back direction.
2. **Pick the engine** before touching dissolved phases: Elastix one-shot (default vs
   6-vox pitch + bending penalty) or ourpca2 with a finer/cubic lattice.
3. Apply the chosen warp to `dissolved_phase_real/imag` (Tyger keys) — not started.

Parked: quiver plots (F39), ANTs MI (F18), production iterations (F14), F12/F13.

## Pointers

| What | Where |
|---|---|
| ourpca2 outputs (big) | `/Volumes/HoomHamExt/Work/Codes/2026_PCA_registration/ourpca2*/<study>/` |
| Elastix outputs | `workspace/outputs/registered/<study>_{gasonly,hierarchical_gasonly}{,_gs6}/` |
| Videos | `workspace/outputs/videos_002BB/{axial,coronal,sagittal}/slice_NNN.mp4` |
| Run a subject through all three | see C12/C5/C6 cards; then `score_all.py --ref <ourpca2 dir> label=path ...` |
| XeCS provenance | `ourpca2/README.md`, `ourpca2/XeCS_DirtyRun_Pipeline.md` |
